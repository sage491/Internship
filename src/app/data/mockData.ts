export const inventoryItems: never[] = [];

export const usedItemsData: never[] = [];

export const requiredItemsData: never[] = [];

export const stockInData: never[] = [];

export const stockOutData: never[] = [];

export const suppliersData: never[] = [];

export const usersData: never[] = [];

export const kpiData = {
  totalItems: 0,
  usedItems: 0,
  requiredItems: 0,
  lowStockItems: 0,
  totalValue: 0,
};

export const usageTrendData: { month: string; issued: number; received: number }[] = [];

export const categoryData: { name: string; value: number; color: string }[] = [];

export const monthlyConsumptionData: { month: string; fuel: number; parts: number; safety: number }[] = [];

export const recentActivity: never[] = [];
